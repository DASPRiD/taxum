comment:
  layout: "header, diff, flags, components"

component_management:
  individual_components:
    - component_id: core
      name: Core
      paths:
        - packages/core/**
    - component_id: jwt
      name: JWT
      paths:
        - packages/jwt/**
